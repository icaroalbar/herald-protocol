{
  "name": "@herald/cli",
  "version": "0.1.0",
  "description": "CLI de referência para provisionar um Herald Outpost e configurar @herald/gateway na sua aplicação.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "herald": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bash -c 'shopt -s globstar; node --test dist/**/*.test.js'"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "herald-protocol",
    "cli",
    "outpost"
  ]
}
